<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="search">
	<select id="search" resultType = "com.care.dto.ReviewDTO">
		 SELECT * FROM  product WHERE production = #{searchItem}
	</select>
	<!--  select modelName from product where production like #{production};  -->
		<!--  검색어에 모델명을 확인할 수 있는 문자가 들어가있으면 나오도록 짜기  -->
</mapper>